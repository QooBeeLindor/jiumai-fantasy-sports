# 🏆 铁人赛系统 - 维护指南 v2.0

**更新日期：** 2026-01-13  
**服务器：** ubuntu@129.204.8.241  
**域名：** jiumaifantasy.online

---

## 📊 系统概况

### 三个已部署系统

| 系统 | 端口 | 路径 | 管理方式 | 状态 |
|------|------|------|----------|------|
| NBA竞价选秀ELO | 5000 | / | Supervisor | ✅ |
| 铁人个人赛 | 5001 | /ironman | Supervisor | ✅ |
| 铁人团队赛 | 5002 | /irongroup | nohup | ✅ |

### 访问地址

**铁人个人赛：**
- http://jiumaifantasy.online/ironman/individual
- http://129.204.8.241/ironman/individual

**铁人团队赛：**
- http://jiumaifantasy.online/irongroup/leaderboard
- http://129.204.8.241/irongroup/leaderboard

**NBA ELO系统：**
- http://jiumaifantasy.online/
- http://129.204.8.241/

---

## 🚨 重要变更（2026-01-13）

### Yahoo API访问问题

**问题：** Yahoo已封锁中国大陆IP访问其API（自2021年11月）

**影响：**
- ❌ 无法从服务器直接同步Yahoo数据
- ✅ 需要在本地同步，然后上传数据库

**解决方案：** 本地同步 + 上传数据库（详见下文）

---

## 📅 每周维护任务（10分钟）

### Step 1: 本地同步数据（5分钟）

**在本地Windows电脑执行：**

```bash
# 同步个人赛
cd /d G:\ironman
python sync_yahoo_standings.py ironman.db oauth2.json

# 同步团队赛
cd /d G:\irongroup
python sync_team_yahoo_simple.py irongroup.db oauth2.json
```

**成功标志：**
```
✓ 成功同步 NHL 数据
✓ 成功同步 NBA 数据
✓ 数据已更新到数据库
```

---

### Step 2: 停止服务（1分钟）

**SSH连接服务器：**

```bash
ssh ubuntu@129.204.8.241

# 停止个人赛
sudo supervisorctl stop ironman

# 停止团队赛
pkill -f "gunicorn.*irongroup"

# 确认已停止
sudo supervisorctl status ironman
ps aux | grep "gunicorn.*irongroup"
```

---

### Step 3: 上传数据库（2分钟）

**用WinSCP：**
1. 连接到 ubuntu@129.204.8.241
2. 上传文件：
   - `G:\ironman\ironman.db` → `/var/www/ironman/ironman.db`
   - `G:\irongroup\irongroup.db` → `/var/www/irongroup/irongroup.db`

**如果遇到权限错误：**
```bash
# SSH执行
sudo chown ubuntu:ubuntu /var/www/ironman/ironman.db
sudo chown ubuntu:ubuntu /var/www/irongroup/irongroup.db
sudo chmod 664 /var/www/ironman/ironman.db
sudo chmod 664 /var/www/irongroup/irongroup.db
```

---

### Step 4: 重启服务（2分钟）

**SSH执行：**

```bash
# 重启个人赛
sudo supervisorctl start ironman

# 重启团队赛
cd /var/www/irongroup
source venv/bin/activate
nohup gunicorn --bind 127.0.0.1:5002 --workers 2 irongroup_app:app > irongroup.log 2>&1 &

# 验证服务
sudo supervisorctl status
ps aux | grep gunicorn
```

---

### Step 5: 验证更新（1分钟）

**浏览器访问：**
- http://jiumaifantasy.online/ironman/individual
- http://jiumaifantasy.online/irongroup/leaderboard

**检查：**
- [ ] NHL数据已更新
- [ ] NBA数据已更新
- [ ] 更新时间正确
- [ ] 排名正确

---

## 🛠️ 服务管理命令

### 查看服务状态

```bash
# 查看所有Supervisor服务
sudo supervisorctl status

# 查看gunicorn进程
ps aux | grep gunicorn

# 查看端口占用
netstat -tlnp | grep -E "5000|5001|5002"
```

### 重启服务

```bash
# 重启个人赛
sudo supervisorctl restart ironman

# 重启团队赛
cd /var/www/irongroup
pkill -f "gunicorn.*irongroup"
source venv/bin/activate
nohup gunicorn --bind 127.0.0.1:5002 --workers 2 irongroup_app:app > irongroup.log 2>&1 &

# 重启NBA ELO
sudo supervisorctl restart nba_elo
```

### 查看日志

```bash
# 个人赛日志
sudo supervisorctl tail ironman stderr
sudo supervisorctl tail ironman stdout

# 团队赛日志
tail -f /var/www/irongroup/irongroup.log

# NBA ELO日志
sudo supervisorctl tail nba_elo stderr
```

---

## 🔧 故障排查

### 问题1: 网站无法访问（502错误）

**原因：** 服务未运行

**解决：**
```bash
# 检查服务
sudo supervisorctl status
ps aux | grep gunicorn

# 重启服务
sudo supervisorctl restart ironman
cd /var/www/irongroup && pkill -f "gunicorn.*irongroup"
cd /var/www/irongroup && source venv/bin/activate && nohup gunicorn --bind 127.0.0.1:5002 --workers 2 irongroup_app:app > irongroup.log 2>&1 &
```

---

### 问题2: 数据未更新

**原因：** 本地同步失败或未上传

**解决：**
1. 检查本地同步日志
2. 确认数据库文件已更新（查看文件修改时间）
3. 重新上传数据库
4. 重启服务

---

### 问题3: 服务器重启后服务未启动

**原因：** 团队赛使用nohup，不会自动启动

**解决：**
```bash
# 手动启动团队赛
cd /var/www/irongroup
source venv/bin/activate
nohup gunicorn --bind 127.0.0.1:5002 --workers 2 irongroup_app:app > irongroup.log 2>&1 &
```

**长期解决：** 配置Supervisor管理团队赛（见优化建议）

---

### 问题4: OAuth过期

**错误信息：** "TOKEN HAS EXPIRED" 或 "Permission denied: oauth2.json"

**解决：**
```bash
# 在服务器上修复权限
cd /var/www/ironman
sudo chown ubuntu:ubuntu oauth2.json
sudo chmod 644 oauth2.json

cd /var/www/irongroup
sudo chown ubuntu:ubuntu oauth2.json
sudo chmod 644 oauth2.json
```

**如果token完全过期：**
1. 在本地重新获取OAuth（运行oauth_setup.py）
2. 上传新的oauth2.json到服务器
3. 设置正确权限

---

## 💾 数据备份

### 手动备份（建议每月一次）

```bash
# SSH连接
ssh ubuntu@129.204.8.241

# 备份个人赛
cd /var/www/ironman
cp ironman.db ironman.db.backup.$(date +%Y%m%d)

# 备份团队赛
cd /var/www/irongroup
cp irongroup.db irongroup.db.backup.$(date +%Y%m%d)

# 下载到本地（在本地电脑执行）
# 用WinSCP下载备份文件
```

### 恢复备份

```bash
# 停止服务
sudo supervisorctl stop ironman
pkill -f "gunicorn.*irongroup"

# 恢复数据库
cd /var/www/ironman
cp ironman.db.backup.20260113 ironman.db

cd /var/www/irongroup
cp irongroup.db.backup.20260113 irongroup.db

# 重启服务
sudo supervisorctl start ironman
cd /var/www/irongroup && source venv/bin/activate && nohup gunicorn --bind 127.0.0.1:5002 --workers 2 irongroup_app:app > irongroup.log 2>&1 &
```

---

## 📁 重要文件位置

### 铁人个人赛

```
/var/www/ironman/
├── ironman_app.py              # Flask应用
├── ironman.db                   # SQLite数据库
├── sync_yahoo_standings.py      # Yahoo同步脚本
├── oauth2.json                  # Yahoo OAuth认证
├── venv/                        # Python虚拟环境
└── templates/                   # HTML模板
    └── individual_leaderboard.html
```

### 铁人团队赛

```
/var/www/irongroup/
├── irongroup_app.py             # Flask应用
├── irongroup.db                 # SQLite数据库
├── sync_team_yahoo_simple.py    # Yahoo同步脚本
├── oauth2.json                  # Yahoo OAuth认证
├── irongroup.log                # 运行日志
├── venv/                        # Python虚拟环境
└── templates/
    ├── landing.html             # 首页
    ├── leaderboard.html         # 排行榜
    └── team_detail.html         # 团队详情
```

### Nginx配置

```
/etc/nginx/sites-available/default
```

### Supervisor配置

```
/etc/supervisor/conf.d/
├── nba_elo.conf
└── ironman.conf
```

---

## 🔄 系统优化建议

### 短期优化（1-2周）

#### 1. 统一使用Supervisor管理

**创建团队赛的Supervisor配置：**

```bash
sudo nano /etc/supervisor/conf.d/irongroup.conf
```

内容：
```ini
[program:irongroup]
command=/var/www/irongroup/venv/bin/gunicorn --bind 127.0.0.1:5002 --workers 2 irongroup_app:app
directory=/var/www/irongroup
user=ubuntu
autostart=true
autorestart=true
stderr_logfile=/var/log/irongroup.err.log
stdout_logfile=/var/log/irongroup.out.log
environment=PATH="/var/www/irongroup/venv/bin"
```

应用配置：
```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start irongroup
```

#### 2. 创建快速更新脚本

**在本地创建 `weekly_update.bat`：**

```batch
@echo off
echo ============================================
echo 铁人赛每周数据更新
echo ============================================
echo.

echo [1/4] 同步个人赛数据...
cd /d G:\ironman
python sync_yahoo_standings.py ironman.db oauth2.json
if errorlevel 1 (
    echo ERROR: 个人赛同步失败
    pause
    exit
)

echo.
echo [2/4] 同步团队赛数据...
cd /d G:\irongroup
python sync_team_yahoo_simple.py irongroup.db oauth2.json
if errorlevel 1 (
    echo ERROR: 团队赛同步失败
    pause
    exit
)

echo.
echo [3/4] 数据同步完成！
echo 请用WinSCP上传以下文件到服务器：
echo   - G:\ironman\ironman.db -^> /var/www/ironman/
echo   - G:\irongroup\irongroup.db -^> /var/www/irongroup/
echo.

echo [4/4] 上传完成后，在SSH中执行：
echo   sudo supervisorctl restart ironman
echo   cd /var/www/irongroup ^&^& pkill -f "gunicorn.*irongroup" ^&^& source venv/bin/activate ^&^& nohup gunicorn --bind 127.0.0.1:5002 --workers 2 irongroup_app:app ^> irongroup.log 2^>^&1 ^&
echo.
echo ============================================
echo 数据库已更新，请按上述步骤上传
echo ============================================
pause
```

---

### 中期优化（1-2月）

#### 1. 考虑迁移到海外服务器

**优点：**
- 可以直接在服务器同步Yahoo数据
- 不需要本地同步+上传的繁琐流程
- 可以配置Cron自动更新

**推荐服务商：**
- AWS（香港、新加坡）
- Google Cloud
- Vultr
- DigitalOcean

#### 2. 添加EPL数据同步（Fantrax）

**需要：**
- Fantrax API访问
- 创建EPL同步脚本

#### 3. 配置SSL证书（HTTPS）

**使用Let's Encrypt免费证书：**
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d jiumaifantasy.online
```

---

### 长期规划（3-6月）

#### 1. 创建Web管理后台

**功能：**
- 查看系统状态
- 手动触发数据同步
- 查看日志
- 数据库备份管理

#### 2. 添加监控和告警

**使用：**
- Prometheus + Grafana
- 或简单的健康检查脚本

#### 3. 数据可视化

**添加：**
- 排名趋势图
- 得分对比
- 历史数据分析

---

## 📋 每周维护清单

- [ ] 本地同步个人赛数据
- [ ] 本地同步团队赛数据
- [ ] 上传数据库到服务器
- [ ] 重启服务
- [ ] 验证网站访问正常
- [ ] 检查数据已更新

## 📋 每月维护清单

- [ ] 备份数据库
- [ ] 检查磁盘空间
- [ ] 清理日志文件
- [ ] 检查OAuth token有效期
- [ ] 更新系统包（可选）

---

## 🆘 紧急联系

如果遇到无法解决的问题：

1. **检查日志：**
   ```bash
   sudo supervisorctl tail ironman stderr
   tail -f /var/www/irongroup/irongroup.log
   ```

2. **重启所有服务：**
   ```bash
   sudo supervisorctl restart all
   cd /var/www/irongroup && pkill -f "gunicorn.*irongroup" && source venv/bin/activate && nohup gunicorn --bind 127.0.0.1:5002 --workers 2 irongroup_app:app > irongroup.log 2>&1 &
   ```

3. **检查Nginx：**
   ```bash
   sudo nginx -t
   sudo systemctl status nginx
   ```

---

## 📖 相关文档

- **项目完整文档：** 项目完整文档_新对话专用.md
- **快速开始：** 快速开始.md
- **系统状态总结：** 系统状态总结.md

---

**版本：** v2.0  
**更新：** 2026-01-13  
**适用：** 中国大陆服务器（Yahoo API被墙）

---

**有问题随时查看此文档！** 🚀
